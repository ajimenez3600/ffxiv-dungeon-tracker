<p id="notice"><%= notice %></p>

<h1>Metrics Raids</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @metrics_raids.each do |metrics_raid| %>
      <tr>
        <td><%= link_to 'Show', metrics_raid %></td>
        <td><%= link_to 'Edit', edit_metrics_raid_path(metrics_raid) %></td>
        <td><%= link_to 'Destroy', metrics_raid, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Metrics Raid', new_metrics_raid_path %>
