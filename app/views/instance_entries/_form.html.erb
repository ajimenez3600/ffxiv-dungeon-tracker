<%= form_with(model: instance_entry) do |form| %>
  <% if instance_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(instance_entry.errors.count, "error") %> prohibited this instance_entry from being saved:</h2>

      <ul>
        <% instance_entry.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
